import { Region } from "./region";

export class Ad {

    constructor(
        title: String,
        description: String,
        region: Region,
        price: Number,
        images: Array<String>
    ) {

    }
}